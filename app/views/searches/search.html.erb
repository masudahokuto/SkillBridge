<div class="container-fluid">
  <div class="row d-flex justify-content-center">
    <div class="col-lg-8">
  
      <h2>検索結果</h2>
      <%= render "searches/search_form" %>
    </div>
  </div>
</div>
  <% if @portfolios.any? %>
    <%= render "public/portfolios/portfolio_cards", portfolios: @portfolios %>
  <% else %>
    <p>一致するポートフォリオが見つかりませんでした。</p>
  <% end %>
</div>